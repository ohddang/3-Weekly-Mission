const DOMAIN = "https://bootcamp-api.codeit.kr";
const USERS = "api/users";
const LINKS = "links";
const FOLDERS = "folders";

const API = {
  DOMAIN,
  USERS,
  LINKS,
  FOLDERS,
};

export default API;
