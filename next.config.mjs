/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["codeit-images.codeit.com", "ca.slack-edge.com", "avatars.githubusercontent.com"],
  },
};

export default nextConfig;
